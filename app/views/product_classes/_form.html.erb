<%= form_with(model: product_class) do |form| %>
  <% if product_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_class.errors.count, "error") %> prohibited this product_class from being saved:</h2>

      <ul>
        <% product_class.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
