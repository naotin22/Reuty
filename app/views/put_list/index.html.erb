<div class="card bg-dark">
    <div class="card-header text-white">
        <h1>出品一覧</h1>
    </div>
</div>




<div>
    <%= link_to image_tag("/assets/put_entry.png"), put_entry_new_path %>
</div>

<table>
    <thead>
        <tr>
            <th>&nbsp;</th>
            <th>商品名</th>
            <th>価格</th>
            <th>&nbsp;</th>
            <th>&nbsp;</th>
        </tr>
    </thead>

    
    <tbody>
        <% @products.each do |product| %>
                <tr>
                    <td width="200px">
                        <% if product.image.present? %>
                            <%= image_tag "product/#{product.image}", {class: "img-fluid"} %>
                        <% end %>
                    </td>
                    <td width="200px"><%= product.name %></td>
                    <td width="100px"><%= product.price %></td>
                    <td><%= link_to '編集', put_entry_edit_path(product.id) %></td>
                    <td><%= link_to '削除', put_entry_destroy_path(product.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
                </tr>
        <% end %>
    </tbody>
</table>

<%# <div class="fixed-bottom mx-0 bg-black bg-opacity-75 text-right text-white">
    <div>
        <%= link_to image_tag("/assets/back.png"), home_path %>
    <%# </div>
</div> %>


<footer class="fixed-bottom mx-0 bg-dark bg-opacity-75 text-right text-white">
    <div class='container mt-5'>
        <div class='row'>
    	    <div>
    	        <%= link_to image_tag("/assets/back.png"), home_path %>
    	    </div>
        </div>
    </div>
</footer>