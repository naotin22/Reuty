<%= form_with(model: report_comment) do |form| %>
  <% if report_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report_comment.errors.count, "error") %> prohibited this report_comment from being saved:</h2>

      <ul>
        <% report_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
